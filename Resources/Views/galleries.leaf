<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" type="text/css" href="/css/dp.css" />
    <script>
        
        function chgImg (imgName, newImg){
            var i = document.getElementsByName(imgName)[0];
            i.src = eval (newImg + ".src");
        }
        
        #for(gallery in galleries):

            img#(gallery.id)Normal = new Image;
            img#(gallery.id)Red = new Image;
            img#(gallery.id)Normal.src = "#(gallery.normalImagePath)";
            img#(gallery.id)Red.src = "#(gallery.redImagePath)";        
        
        #endfor
        
    </script>
</head>
<body>
        
    <h2>The Photo Collections...</h2><p>Click on any of the pictures in this part of the page to open the whole set of photos for that collection.  If this is your first trip in, give it a minute.  It may take this page a little while to load all of the pictures, but I like having one big giant list of my photo albums in one place.</p>

    <table border="0" cellpadding="2em" cellspacing="0" width="100%" height="1" style="text-align:center";>
    #for(gallery in galleries):
        #if(gallery.column == "left"):
            <tr><td width="50%" height="113">
                    <a href="#(gallery.path)" onmouseover="chgImg ('i#(gallery.id)','img#(gallery.id)Red');" onmouseout="chgImg ('i#(gallery.id)','img#(gallery.id)Normal');" target="_blank">
                        <img border="0" src="#(gallery.normalImagePath)" height="109" name="i#(gallery.id)">
                    </a>
                    <div class="igal-caption">#(gallery.name)</div>
                </td>
        #elseif(gallery.column == "right"):
                <td width="50%" height="113">
                    <a href="#(gallery.path)" onmouseover="chgImg ('i#(gallery.id)','img#(gallery.id)Red');" onmouseout="chgImg ('i#(gallery.id)','img#(gallery.id)Normal');" target="_blank">
                        <img border="0" src="#(gallery.normalImagePath)" height="109" name="i#(gallery.id)">
                    </a>
                    <div class="igal-caption">#(gallery.name)</div>
                </td>
            </tr>
        #endif

    #endfor
    
    </table>
    

</body>
